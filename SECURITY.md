# Security Policy

apfel-tag runs entirely on-device. It reads text from standard input, classifies
it with Apple's on-device content-tagging model, and writes tags to standard
output. It makes no network connections and stores no data.

## Reporting

Report security issues to arti.ficial@fullstackoptimization.com.
